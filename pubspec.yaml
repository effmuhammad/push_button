name: push_button
description: "Flutter package to easily make push button with push down effect animation."
version: 0.0.1
homepage: https://effdevstudio.web.app
repository: https://github.com/effmuhammad/push_button
issue_tracker: https://github.com/effmuhammad/push_button/issues

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows: